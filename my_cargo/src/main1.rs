fn main(){ // ){ -> they should stay togather
    println!("Hello World!"); // ! = macro 
    // function = println();
}
// rust will run only main function as like c or c++

// whole code can be formatted by using $ rustfmt filename $ in terminal

// before ruuning the code we need to compile : $ rustc filename then $ ./filename
